import BackdropProvider, {
  BackdropProviderProps,
  BackdropContext,
  BackdropContextProps
} from '@mgcrea/react-native-backdrop-provider';
import ModalDialog, {Props as ModalDialogProps, Handle as ModalDialogHandle} from './ModalDialog';

export default ModalDialog;
export {ModalDialogProps, ModalDialogHandle};
export {BackdropProvider, BackdropProviderProps, BackdropContext, BackdropContextProps};
